This is an example application for the tsuru PAAS using lisp and wookie.

It demostrates how to use requirements.apt (for apt package dependencies), requirements.txt (for requirements that are outside quicklist, or for things you want to override in quicklisp).
